# Web framework — the API itself
fastapi==0.115.0

# ASGI server that runs FastAPI
uvicorn[standard]==0.30.6

# ORM (turns Python objects into SQL)
sqlalchemy==2.0.35
# Postgres driver used by SQLAlchemy
psycopg[binary]==3.2.3

# Reads .env files when running locally without Docker
python-dotenv==1.0.1

# Password hashing.
# `passlib` 1.7.4 is incompatible with bcrypt >=4.1, so pin both.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# JSON Web Tokens for the session
pyjwt==2.9.0

# Data validation (FastAPI uses this for request bodies)
pydantic[email]==2.9.2
pydantic-settings==2.5.2

# --- dev / test dependencies ---
pytest==8.3.3
httpx==0.27.2
ruff==0.6.9
